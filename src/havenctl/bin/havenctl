#!/usr/bin/env python

import sys

try:
    from havenctl import launcher
except ImportError:
    sys.exit("Error: Could not find havenctl python package. Try to reinstall?")

if __name__ == "__main__":
    launcher.execute_from_command_line()
